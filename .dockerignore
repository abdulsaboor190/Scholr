# Don't send these to Docker — they get regenerated inside
node_modules
dist
.git

# Local env (Railway provides these via dashboard variables)
.env
.env.*
!.env.example

# Editor/OS files
.DS_Store
*.md
coverage
